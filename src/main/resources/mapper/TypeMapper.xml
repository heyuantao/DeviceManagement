<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.heyuantao.devicemanagement.mapper.TypeMapper">
    <select id="selectByParams" parameterType="java.util.Map" resultType="cn.heyuantao.devicemanagement.domain.Type">
        select * from type
        <where>
            <if test="selectMap!=null">
                <if test="selectMap.name!=null  and selectMap.name!=''">
                    and name = #{selectMap.name}
                </if>
                <if test="selectMap.description!=null  and selectMap.description!=''">
                    and description = #{selectMap.description}
                </if>
            </if>
            <if test="searchMap!=null and searchMap.search!=null">
                <bind name="searchValue" value="'%'+searchMap.search+'%'"/>
                <if test="searchMap.search!=null  and searchMap.search!=''">
                    and name like #{searchValue}
                </if>
            </if>
        </where>
    </select>

</mapper>